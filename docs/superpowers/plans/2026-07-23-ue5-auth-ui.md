# UE5 인증 UI Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 기존 `Auth.*` 콘솔 명령으로만 조작하던 FastAPI 인증 통신을, Play(PIE) 시 자동으로 뜨는 화면 UI(버튼/입력란)로 조작할 수 있게 한다.

**Architecture:** 위젯 로직은 전부 C++(`UAuthWidgetBase : UUserWidget`)에 두고 WBP는 비주얼 껍데기만 담당한다. 표시할 WBP는 `UDeveloperSettings`(Project Settings)로 지정하고, `UAuthSubsystem`이 월드 BeginPlay 시점에 위젯을 생성·뷰포트에 추가한다. 기존 `UAuthSubsystem`의 `FAuthCallback`(TFunction) API를 그대로 호출한다.

**Tech Stack:** Unreal Engine 5.7 (C++), UMG/Slate, DeveloperSettings 모듈. 기존 HTTP/Json 통신 계층 재사용.

## Global Constraints

- 엔진: UE 5.7. Target.cs는 `BuildSettingsVersion.V6` (기존 유지).
- 콘솔 명령(`Auth.*`)과 기존 `AuthConsoleCommands.cpp`는 **삭제하지 않는다** — UI와 병존.
- `UAuthSubsystem`의 공개 API 시그니처(`Health/Signup/Login/GetMe/DeleteMe`, `IsLoggedIn`)는 변경 금지. UI는 이를 그대로 소비한다.
- BindWidget 위젯 이름은 스펙 4절과 **정확히 일치**해야 한다: `EmailBox`, `UsernameBox`, `PasswordBox`, `SignupButton`, `LoginButton`, `MeButton`, `DeleteButton`, `HealthButton`, `StatusText`, `LoginStateText`(Optional).
- 비동기 콜백에서 위젯 접근 시 `TWeakObjectPtr`로 유효성 확인(Use-After-Free 방지).
- 자동화 테스트는 UE HTTP/UMG 특성상 이 범위에서 제외(기존 스펙과 동일 전략). 검증은 **컴파일 성공 + 수동 PIE**.
- **빌드/실행은 사용자 환경(Visual Studio + UE 5.7 에디터)에서 수행**한다. 각 태스크의 "빌드 확인"은 사용자 환경 기준이다.
- 파일 인코딩/스타일은 기존 `Auth/` 파일과 일치(한글 주석 UTF-8, 탭 들여쓰기, same-folder include).

---

## File Structure

**신규 C++**
- `Source/VibeProjectTest/Auth/AuthUISettings.h` — `UAuthUISettings : UDeveloperSettings`. Play 시 표시할 위젯 클래스 하나 보관.
- `Source/VibeProjectTest/Auth/AuthWidgetBase.h` / `.cpp` — `UAuthWidgetBase : UUserWidget`. 버튼 핸들러, 결과 표시, 로그인 상태 기반 버튼 활성화.

**기존 수정**
- `Source/VibeProjectTest/VibeProjectTest.Build.cs` — `UMG`, `Slate`, `SlateCore`, `DeveloperSettings` 의존성 추가.
- `Source/VibeProjectTest/Auth/AuthSubsystem.h` / `.cpp` — `Initialize`/`Deinitialize` override + 월드 BeginPlay 구독 → 자동 표시 헬퍼.

---

## Task 1: 모듈 의존성 + AuthUISettings (표시할 위젯 지정 창구)

**Files:**
- Modify: `Source/VibeProjectTest/VibeProjectTest.Build.cs`
- Create: `Source/VibeProjectTest/Auth/AuthUISettings.h`

**Interfaces:**
- Consumes: 없음.
- Produces: `UAuthUISettings` (UDeveloperSettings, `config=Game`), 멤버 `TSoftClassPtr<UUserWidget> AuthWidgetClass`. `GetDefault<UAuthUISettings>()`로 읽는다. Project Settings 카테고리 "Game" → "Auth UI".

- [ ] **Step 1: Build.cs에 UMG/Slate/DeveloperSettings 추가**

`PublicDependencyModuleNames`를 아래로 교체:

```csharp
		PublicDependencyModuleNames.AddRange(new string[]
		{
			"Core", "CoreUObject", "Engine", "InputCore",
			"UMG", "Slate", "SlateCore", "DeveloperSettings"
		});
```

- [ ] **Step 2: AuthUISettings.h 작성**

`Source/VibeProjectTest/Auth/AuthUISettings.h`:

```cpp
#pragma once

#include "CoreMinimal.h"
#include "Engine/DeveloperSettings.h"
#include "AuthUISettings.generated.h"

class UUserWidget;

/**
 * Play(PIE) 시작 시 자동 표시할 Auth 위젯(WBP)을 Project Settings에서 지정한다.
 * Project Settings > Game > Auth UI 에 노출된다.
 */
UCLASS(config = Game, defaultconfig, meta = (DisplayName = "Auth UI"))
class VIBEPROJECTTEST_API UAuthUISettings : public UDeveloperSettings
{
	GENERATED_BODY()

public:
	/** 뷰포트에 추가할 위젯 클래스. AuthWidgetBase로 reparent한 WBP를 지정한다. 미지정 시 UI 미표시. */
	UPROPERTY(config, EditAnywhere, Category = "Auth UI", meta = (AllowAbstract = false))
	TSoftClassPtr<UUserWidget> AuthWidgetClass;

	virtual FName GetCategoryName() const override { return TEXT("Game"); }
};
```

- [ ] **Step 3: 빌드 확인 (사용자 환경)**

Visual Studio 또는 에디터에서 프로젝트 리빌드.
Expected: 컴파일 성공. 에디터 실행 후 **Project Settings > Game > Auth UI > Auth Widget Class** 드롭다운이 보이면 성공(아직 지정 안 함).

- [ ] **Step 4: Commit**

```bash
git add Source/VibeProjectTest/VibeProjectTest.Build.cs Source/VibeProjectTest/Auth/AuthUISettings.h
git commit -m "feat(auth-ui): add UMG deps and AuthUISettings for widget selection"
```

---

## Task 2: AuthWidgetBase — UI 로직 (C++)

**Files:**
- Create: `Source/VibeProjectTest/Auth/AuthWidgetBase.h`
- Create: `Source/VibeProjectTest/Auth/AuthWidgetBase.cpp`

**Interfaces:**
- Consumes: `UAuthSubsystem`의 `Signup/Login/GetMe/DeleteMe/Health(..., FAuthCallback)` 및 `IsLoggedIn()`(AuthSubsystem.h). `FAuthCallback = TFunction<void(bool, int32, const FString&)>`.
- Produces: `UAuthWidgetBase : UUserWidget`. WBP가 이 클래스로 reparent됨. Task 3(subsystem)은 이 클래스를 직접 참조하지 않고 `UUserWidget`으로만 생성한다.

- [ ] **Step 1: AuthWidgetBase.h 작성**

`Source/VibeProjectTest/Auth/AuthWidgetBase.h`:

```cpp
#pragma once

#include "CoreMinimal.h"
#include "Blueprint/UserWidget.h"
#include "AuthWidgetBase.generated.h"

class UButton;
class UEditableTextBox;
class UTextBlock;
class UAuthSubsystem;

/**
 * FastAPI 인증 서버를 조작하는 화면 UI.
 * WBP를 이 클래스로 reparent하고 아래 BindWidget 이름들로 위젯을 배치한다(레이아웃은 자유).
 */
UCLASS()
class VIBEPROJECTTEST_API UAuthWidgetBase : public UUserWidget
{
	GENERATED_BODY()

protected:
	virtual void NativeConstruct() override;

	// --- 입력란 (WBP에서 정확한 이름으로 배치) ---
	UPROPERTY(meta = (BindWidget)) TObjectPtr<UEditableTextBox> EmailBox;
	UPROPERTY(meta = (BindWidget)) TObjectPtr<UEditableTextBox> UsernameBox;
	UPROPERTY(meta = (BindWidget)) TObjectPtr<UEditableTextBox> PasswordBox;

	// --- 버튼 ---
	UPROPERTY(meta = (BindWidget)) TObjectPtr<UButton> SignupButton;
	UPROPERTY(meta = (BindWidget)) TObjectPtr<UButton> LoginButton;
	UPROPERTY(meta = (BindWidget)) TObjectPtr<UButton> MeButton;
	UPROPERTY(meta = (BindWidget)) TObjectPtr<UButton> DeleteButton;
	UPROPERTY(meta = (BindWidget)) TObjectPtr<UButton> HealthButton;

	// --- 표시 ---
	UPROPERTY(meta = (BindWidget)) TObjectPtr<UTextBlock> StatusText;
	UPROPERTY(meta = (BindWidgetOptional)) TObjectPtr<UTextBlock> LoginStateText;

	UFUNCTION() void OnSignupClicked();
	UFUNCTION() void OnLoginClicked();
	UFUNCTION() void OnMeClicked();
	UFUNCTION() void OnDeleteClicked();
	UFUNCTION() void OnHealthClicked();

private:
	UAuthSubsystem* GetAuth() const;
	void RefreshButtons();
	/** 콜백 결과를 StatusText에 표시하고 버튼 상태를 갱신한다. */
	void SetStatus(bool bSuccess, int32 StatusCode, const FString& Content);
};
```

- [ ] **Step 2: AuthWidgetBase.cpp — 생성/헬퍼 작성**

`Source/VibeProjectTest/Auth/AuthWidgetBase.cpp`:

```cpp
#include "AuthWidgetBase.h"
#include "AuthSubsystem.h"
#include "Components/Button.h"
#include "Components/EditableTextBox.h"
#include "Components/TextBlock.h"
#include "Engine/GameInstance.h"
#include "Engine/World.h"

void UAuthWidgetBase::NativeConstruct()
{
	Super::NativeConstruct();

	if (SignupButton) { SignupButton->OnClicked.AddDynamic(this, &UAuthWidgetBase::OnSignupClicked); }
	if (LoginButton)  { LoginButton->OnClicked.AddDynamic(this, &UAuthWidgetBase::OnLoginClicked); }
	if (MeButton)     { MeButton->OnClicked.AddDynamic(this, &UAuthWidgetBase::OnMeClicked); }
	if (DeleteButton) { DeleteButton->OnClicked.AddDynamic(this, &UAuthWidgetBase::OnDeleteClicked); }
	if (HealthButton) { HealthButton->OnClicked.AddDynamic(this, &UAuthWidgetBase::OnHealthClicked); }

	RefreshButtons();
}

UAuthSubsystem* UAuthWidgetBase::GetAuth() const
{
	if (const UWorld* World = GetWorld())
	{
		if (UGameInstance* GI = World->GetGameInstance())
		{
			return GI->GetSubsystem<UAuthSubsystem>();
		}
	}
	return nullptr;
}

void UAuthWidgetBase::RefreshButtons()
{
	const UAuthSubsystem* Auth = GetAuth();
	const bool bLoggedIn = Auth && Auth->IsLoggedIn();

	if (MeButton)     { MeButton->SetIsEnabled(bLoggedIn); }
	if (DeleteButton) { DeleteButton->SetIsEnabled(bLoggedIn); }
	if (LoginStateText)
	{
		LoginStateText->SetText(FText::FromString(bLoggedIn ? TEXT("로그인됨") : TEXT("로그아웃 상태")));
	}
}

void UAuthWidgetBase::SetStatus(bool bSuccess, int32 StatusCode, const FString& Content)
{
	if (StatusText)
	{
		const TCHAR* Mark = bSuccess ? TEXT("O") : TEXT("X");
		StatusText->SetText(FText::FromString(FString::Printf(TEXT("%s [%d] %s"), Mark, StatusCode, *Content)));
	}
	RefreshButtons();
}
```

- [ ] **Step 3: AuthWidgetBase.cpp — 버튼 핸들러 작성 (같은 파일에 이어서)**

```cpp
void UAuthWidgetBase::OnSignupClicked()
{
	UAuthSubsystem* Auth = GetAuth();
	if (!Auth) { return; }

	const FString Email    = EmailBox    ? EmailBox->GetText().ToString()    : FString();
	const FString Username = UsernameBox ? UsernameBox->GetText().ToString() : FString();
	const FString Password = PasswordBox ? PasswordBox->GetText().ToString() : FString();
	if (Email.IsEmpty() || Username.IsEmpty() || Password.IsEmpty())
	{
		if (StatusText) { StatusText->SetText(FText::FromString(TEXT("email/username/password를 모두 입력하세요"))); }
		return;
	}

	TWeakObjectPtr<UAuthWidgetBase> WeakThis(this);
	Auth->Signup(Email, Username, Password,
		[WeakThis](bool bOk, int32 Code, const FString& Content)
		{
			if (UAuthWidgetBase* Self = WeakThis.Get()) { Self->SetStatus(bOk, Code, Content); }
		});
}

void UAuthWidgetBase::OnLoginClicked()
{
	UAuthSubsystem* Auth = GetAuth();
	if (!Auth) { return; }

	const FString Email    = EmailBox    ? EmailBox->GetText().ToString()    : FString();
	const FString Password = PasswordBox ? PasswordBox->GetText().ToString() : FString();
	if (Email.IsEmpty() || Password.IsEmpty())
	{
		if (StatusText) { StatusText->SetText(FText::FromString(TEXT("email/password를 입력하세요"))); }
		return;
	}

	TWeakObjectPtr<UAuthWidgetBase> WeakThis(this);
	Auth->Login(Email, Password,
		[WeakThis](bool bOk, int32 Code, const FString& Content)
		{
			if (UAuthWidgetBase* Self = WeakThis.Get()) { Self->SetStatus(bOk, Code, Content); }
		});
}

void UAuthWidgetBase::OnMeClicked()
{
	UAuthSubsystem* Auth = GetAuth();
	if (!Auth) { return; }

	TWeakObjectPtr<UAuthWidgetBase> WeakThis(this);
	Auth->GetMe(
		[WeakThis](bool bOk, int32 Code, const FString& Content)
		{
			if (UAuthWidgetBase* Self = WeakThis.Get()) { Self->SetStatus(bOk, Code, Content); }
		});
}

void UAuthWidgetBase::OnDeleteClicked()
{
	UAuthSubsystem* Auth = GetAuth();
	if (!Auth) { return; }

	TWeakObjectPtr<UAuthWidgetBase> WeakThis(this);
	Auth->DeleteMe(
		[WeakThis](bool bOk, int32 Code, const FString& Content)
		{
			if (UAuthWidgetBase* Self = WeakThis.Get()) { Self->SetStatus(bOk, Code, Content); }
		});
}

void UAuthWidgetBase::OnHealthClicked()
{
	UAuthSubsystem* Auth = GetAuth();
	if (!Auth) { return; }

	TWeakObjectPtr<UAuthWidgetBase> WeakThis(this);
	Auth->Health(
		[WeakThis](bool bOk, int32 Code, const FString& Content)
		{
			if (UAuthWidgetBase* Self = WeakThis.Get()) { Self->SetStatus(bOk, Code, Content); }
		});
}
```

- [ ] **Step 4: 빌드 확인 (사용자 환경)**

프로젝트 리빌드.
Expected: 컴파일 성공. (아직 표시 훅이 없어 화면에는 안 뜸 — Task 3에서 연결.)

- [ ] **Step 5: Commit**

```bash
git add Source/VibeProjectTest/Auth/AuthWidgetBase.h Source/VibeProjectTest/Auth/AuthWidgetBase.cpp
git commit -m "feat(auth-ui): add AuthWidgetBase widget logic (buttons, status, login-gated buttons)"
```

---

## Task 3: AuthSubsystem 자동 표시 훅

**Files:**
- Modify: `Source/VibeProjectTest/Auth/AuthSubsystem.h`
- Modify: `Source/VibeProjectTest/Auth/AuthSubsystem.cpp`

**Interfaces:**
- Consumes: `UAuthUISettings::AuthWidgetClass`(Task 1), `UUserWidget`(Task 2가 만든 WBP를 `UUserWidget`으로 생성). `FWorldDelegates::OnWorldInitializedActors`(`const FActorsInitializedParams&`, `.World` 보유), `UWorld::OnWorldBeginPlay`(멤버, 파라미터 없음) — UE 5.7 `Engine/World.h`에서 확인됨.
- Produces: Play 시 위젯 자동 표시. 외부에 새 공개 API 추가 없음.

- [ ] **Step 1: AuthSubsystem.h 에 override + 멤버 추가**

`class UAuthSubsystem`의 `public:` 섹션 상단(설정/상태 위 또는 아래)에 추가:

```cpp
	// --- 서브시스템 수명주기: 월드 BeginPlay에 Auth UI 자동 표시 ---
	virtual void Initialize(FSubsystemCollectionBase& Collection) override;
	virtual void Deinitialize() override;
```

`private:` 섹션의 `SendRequest` 선언 아래에 추가:

```cpp
	/** 게임 월드 액터 초기화 시점을 잡아 그 월드의 BeginPlay 구독으로 넘긴다. */
	void HandleActorsInitialized(const FActorsInitializedParams& Params);
	/** 월드 BeginPlay 시점에 Auth 위젯을 생성해 뷰포트에 올린다. */
	void HandleWorldBeginPlay();

	FDelegateHandle ActorsInitHandle;
	TWeakObjectPtr<UWorld> PendingWorld;
	TWeakObjectPtr<class UUserWidget> AuthWidget;
```

파일 상단 include 아래에 전방선언 추가(이미 `class UWorld;`는 CoreMinimal로 알려짐 — struct만 추가):

```cpp
struct FActorsInitializedParams;
```

(이 전방선언은 `#include "AuthSubsystem.generated.h"` **위**, 다른 include 아래에 둔다.)

- [ ] **Step 2: AuthSubsystem.cpp 에 include 추가**

파일 상단 기존 include 블록에 추가:

```cpp
#include "AuthUISettings.h"
#include "Blueprint/UserWidget.h"
#include "Engine/World.h"
#include "Engine/GameInstance.h"
#include "GameFramework/PlayerController.h"
```

- [ ] **Step 3: AuthSubsystem.cpp 에 수명주기 + 표시 로직 추가**

`DEFINE_LOG_CATEGORY(LogAuth);` 아래에 추가:

```cpp
void UAuthSubsystem::Initialize(FSubsystemCollectionBase& Collection)
{
	Super::Initialize(Collection);
	ActorsInitHandle = FWorldDelegates::OnWorldInitializedActors.AddUObject(
		this, &UAuthSubsystem::HandleActorsInitialized);
}

void UAuthSubsystem::Deinitialize()
{
	FWorldDelegates::OnWorldInitializedActors.Remove(ActorsInitHandle);
	Super::Deinitialize();
}

void UAuthSubsystem::HandleActorsInitialized(const FActorsInitializedParams& Params)
{
	UWorld* World = Params.World;
	if (!World || !World->IsGameWorld())
	{
		return; // 에디터 프리뷰 월드 등은 무시
	}
	PendingWorld = World;
	World->OnWorldBeginPlay.AddUObject(this, &UAuthSubsystem::HandleWorldBeginPlay);
}

void UAuthSubsystem::HandleWorldBeginPlay()
{
	UWorld* World = PendingWorld.Get();
	if (!World) { return; }
	if (AuthWidget.IsValid()) { return; } // 중복 생성 방지

	const UAuthUISettings* Settings = GetDefault<UAuthUISettings>();
	TSubclassOf<UUserWidget> WidgetClass = Settings ? Settings->AuthWidgetClass.LoadSynchronous() : nullptr;
	if (!WidgetClass)
	{
		UE_LOG(LogAuth, Warning,
			TEXT("Auth 위젯 클래스 미지정 — Project Settings > Game > Auth UI 에서 설정하세요"));
		return;
	}

	UUserWidget* Widget = CreateWidget<UUserWidget>(World, WidgetClass);
	if (!Widget) { return; }
	Widget->AddToViewport();
	AuthWidget = Widget;

	if (APlayerController* PC = World->GetFirstPlayerController())
	{
		PC->bShowMouseCursor = true;
		FInputModeGameAndUI Mode;
		Mode.SetLockMouseToViewportBehavior(EMouseLockMode::DoNotLock);
		PC->SetInputMode(Mode);
	}

	UE_LOG(LogAuth, Log, TEXT("Auth UI 위젯 표시됨"));
}
```

- [ ] **Step 4: 빌드 확인 (사용자 환경)**

프로젝트 리빌드.
Expected: 컴파일 성공. 아직 WBP를 지정하지 않았으면 Play 시 로그에 "Auth 위젯 클래스 미지정" 경고가 뜨는 게 정상.

- [ ] **Step 5: Commit**

```bash
git add Source/VibeProjectTest/Auth/AuthSubsystem.h Source/VibeProjectTest/Auth/AuthSubsystem.cpp
git commit -m "feat(auth-ui): auto-show Auth widget on world BeginPlay via subsystem"
```

---

## Task 4: 에디터 셋업 + PIE 수동 검증 (사용자 수행)

**Files:** (에디터 애셋 — 코드 변경 없음)

**Interfaces:**
- Consumes: Task 1~3 빌드 산출물(`AuthWidgetBase`, `AuthUISettings`, 자동표시 훅).
- Produces: 지정된 WBP가 Play 시 화면에 뜨고 5개 기능이 동작.

- [ ] **Step 1: WBP 생성 및 reparent**

콘텐츠 브라우저에서 Widget Blueprint 생성(예: `WBP_Auth`) → 그래프 상단 **File > Reparent Blueprint** 또는 Class Settings에서 Parent Class를 `AuthWidgetBase`로 변경.

- [ ] **Step 2: 위젯 배치 (정확한 이름)**

Designer에서 아래 이름으로 배치(레이아웃 자유):
- `UEditableTextBox`: **EmailBox**, **UsernameBox**, **PasswordBox**(Is Password 체크 권장)
- `UButton`: **SignupButton**, **LoginButton**, **MeButton**, **DeleteButton**, **HealthButton** (각 버튼 안에 라벨 TextBlock은 자유)
- `UTextBlock`: **StatusText**, (선택) **LoginStateText**

이름이 정확히 일치해야 컴파일/실행 시 바인딩된다(불일치 시 위젯 컴파일 에러 또는 미표시).

- [ ] **Step 3: Project Settings에서 위젯 클래스 지정**

**Edit > Project Settings > Game > Auth UI > Auth Widget Class** = 방금 만든 `WBP_Auth`.

- [ ] **Step 4: 서버 기동**

별도 터미널: `cd C:\Work\VibeCoding` 후 `.\.venv\Scripts\uvicorn.exe app.main:app --reload` (MySQL authdb 필요). 서버 주소가 기본값과 다르면 콘솔에서 `Auth.SetBaseUrl <url>`로 조정 가능(기본 `http://192.168.0.108:8000`).

- [ ] **Step 5: PIE 수동 검증 (스펙 8절)**

Play 후 확인:
- Play 시 UI 자동 표시, 마우스 커서 보이고 버튼 클릭 가능.
- 로그아웃 상태에서 **Me·Delete 버튼 비활성**.
- 회원가입(3칸 입력 → Signup) → `StatusText`에 `O [200/201] ...` 표시.
- 로그인(email/password → Login) 성공 시 **Me·Delete 활성화**, `LoginStateText`가 "로그인됨"으로.
- Me 조회 → `O [200] {내 정보}`.
- Delete → 성공 후 **Me·Delete 재비활성화**, "로그아웃 상태"로.
- Health → `O [200] ...`.
- 서버 미기동 시 각 요청에서 `X [0] network failure` 표시.
- 각 단계 `StatusText`가 `LogAuth` 로그(Output Log)와 일치.

- [ ] **Step 6: 진행 문서 갱신 및 commit (선택)**

`docs/progress.md`에 UI 전환 완료 기록 후:

```bash
git add docs/progress.md
git commit -m "docs: mark Auth UI (cmd→UI) complete after PIE verification"
```

---

## Self-Review 결과

- **스펙 커버리지:** 스펙 3절(신규/수정 파일) → Task 1~3, 4절(BindWidget 이름) → Task 2 + Task 4 Step 2, 5절(동작 흐름) → Task 2, 6절(자동 표시) → Task 3, 7절(에디터 작업) → Task 4, 8절(검증) → Task 4 Step 5, 9절(범위 밖: Base URL 입력란 등) → 미구현으로 유지. 전부 매핑됨.
- **Placeholder:** 없음. 모든 코드 스텝에 완전한 코드 포함.
- **타입 일관성:** `AuthWidgetClass`(TSoftClassPtr<UUserWidget>) → Task 3에서 `LoadSynchronous()`로 `TSubclassOf<UUserWidget>` 취득, 이름 일치. 콜백 시그니처 `FAuthCallback(bool,int32,const FString&)` → 모든 핸들러/`SetStatus`와 일치.
